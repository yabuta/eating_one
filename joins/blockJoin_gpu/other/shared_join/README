JOINをGPUによって高速化する

JOINで行うループの処理をGPuで並列化している。
仕様としてはGPU側にメモリをとり、タプルを移行、処理を行った後結果をダウンロードする。
共有メモリ使用
JOINするテーブルをスレッドグループに分割。分割したrightテーブルをshared_memoryに格納する。また、leftテーブルは使用するデータをローカルメモリに格納する



このディレクトリの説明

Makefile
NestLoopJoinArg.cc
Join_gpuArg.cu
tuple.h

がメインのファイル。

makeするとNestLoopExeが作成される。

exe.shではテーブル数が
5*5,
10*10,
50*50,
100*100,
300*300,
500*500,
1000*1000,
2000*2000,
5000*5000,
10000*10000
のものが実行される。
